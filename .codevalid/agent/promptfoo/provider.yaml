- id: litellm:chat:gpt-5.1
  config:
    apiBaseUrl: "{{ env.LITELLM_BASE_URL }}"
    apiKey: "{{ env.LITELLM_API_KEY }}"
- id: litellm:chat:claude-haiku-4-5
  config:
    apiBaseUrl: "{{ env.LITELLM_BASE_URL }}"
    apiKey: "{{ env.LITELLM_API_KEY }}"